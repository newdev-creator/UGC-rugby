{{ include('partials/main.html.twig') }}

<head>

    {{ include('partials/title-meta.html.twig', {title: 'Evénement'}) }}

    {{ include('partials/head-css.html.twig') }}

</head>

<body>

<!-- Begin page -->
<div id="layout-wrapper">

    {{ include('partials/menu.html.twig') }}

    <!-- ============================================================== -->
    <!-- Start right Content here -->
    <!-- ============================================================== -->
    <div class="main-content">
        <div class="page-content">
            <div class="container-fluid">

                <div class="row">
                    <div class="col-lg-12">
                        <div class="card mt-n4 mx-n4">
                            <div class="bg-soft-warning">
                                <div class="card-body pb-0 px-4">
                                    <div class="row mb-3">
                                        <div class="col-md">
                                            <div class="row align-items-center g-3">
                                                <div class="col-md-auto">
                                                    <div class="avatar-md">
                                                        <div class="avatar-title bg-white rounded-circle">
                                                            <img src="/images/brands/slack.png" alt="" class="avatar-xs">
                                                        </div>
                                                    </div>
                                                </div>
                                                <div class="col-md">
                                                    <div>
                                                        <h4 class="fw-bold">{{ event.title }}</h4>
                                                        <div class="hstack gap-3 flex-wrap">
                                                            <div><i class="ri-building-line align-bottom me-1"></i> {{ event.address }} {{ event.postalCode }} {{ event.city }}</div>
                                                            <div class="vr"></div>
                                                            <div>Date de l'événement : <span class="fw-medium">{{ event.date|date('d/m/Y à H:i') }}</span></div>
                                                            <div class="vr"></div>
                                                            <div>Catégories :</div>
                                                            {% for category in event.categories %}
                                                                <div class="badge rounded-pill bg-danger fs-12">{{ category.name }}</div>
                                                            {% endfor %}
                                                            <div>Statut :</div>
                                                            {% include ('partials/_status.html.twig') %}
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <ul class="nav nav-tabs-custom border-bottom-0" role="tablist">
                                        <li class="nav-item">
                                            <a class="nav-link active fw-semibold" data-bs-toggle="tab" href="#project-overview" role="tab">
                                                Événement
                                            </a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link fw-semibold" data-bs-toggle="tab" href="#event-subscribe" role="tab">
                                                Enfants inscrits
                                            </a>
                                        </li>
                                        <li class="nav-item">
                                            <a class="nav-link fw-semibold" data-bs-toggle="tab" href="#event-carpool" role="tab">
                                                Covoiturage
                                            </a>
                                        </li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12">
                        <div class="tab-content text-muted">
                            <div class="tab-pane fade show active" id="project-overview" role="tabpanel">
                                <div class="row">
                                    <div class="col-xl-9 col-lg-8">
                                        {% include ('partials/_flash_message.html.twig') %}
                                        {% include ('/site/event/_partials/_description.html.twig') %}
                                        {% include ('/site/event/_partials/_registration.html.twig') %}
                                    </div>
                                    <div class="col-xl-3 col-lg-4">
                                        {% include ('/site/event/_partials/_carpools.html.twig') %}
                                    </div>
                                </div>
                            </div>
                            {% include ('/site/event/_partials/_event_subscribe.html.twig') %}
                            {% include ('/site/event/_partials/_event_carpool.html.twig') %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{ include('partials/footer.html.twig') }}
    </div>
</div>
{% include ('/site/event/_partials/_modal.html.twig') %}
{{ include('partials/vendor-scripts.html.twig') }}
{% block javascripts %}
    <script src="{{ asset('/js/pages/project-overview.init.js') }}"></script>
    <!-- App js -->
    <script src="{{ asset('/js/app.js') }}"></script>
{% endblock %}
</body>
</html>